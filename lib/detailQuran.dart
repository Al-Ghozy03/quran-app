import 'package:flutter/material.dart';

class DetailQuran extends StatelessWidget {
  const DetailQuran({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text("ini detail quran"),
      ),
    );
  }
}
