import 'package:flutter/material.dart';

class DetailQuran extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text("ini detail quran"),
      ),
    );
  }
}
